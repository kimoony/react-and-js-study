// const arr = ["a", "b", "c"];

// for (let i = 0; i < arr.length; i++) {
//   console.log(arr[i]);
// }

const person = {
  name: "Hoon",
  age: 34,
  tall: 174
};

const personKeys = Object.keys(person); // 객체 안에 key 값을 순서대로 배열로 바꿔줌
// console.log(personKeys)

for (let i = 0; i < personKeys.length; i++) {
  // console.log(personKeys[i])
}
